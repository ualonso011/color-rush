package com.gentleai.colorrush.audio

/**
 * Types of sound effects used in the game.
 *
 * Each type maps to a raw audio resource loaded by [AudioManager].
 */
enum class SfxType {
    TAP_GREEN,
    TAP_RED,
    TAP_YELLOW,
    GAME_OVER,
}
